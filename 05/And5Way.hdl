CHIP And5Way {
    IN in[5];
    OUT out;

    PARTS:
    And(a=in[0], b=in[1], out=and1);
    And(a=and1, b=in[2], out=and2);
    And(a=and2, b=in[3], out=and3);
    And(a=and3, b=in[4], out=out);
}
